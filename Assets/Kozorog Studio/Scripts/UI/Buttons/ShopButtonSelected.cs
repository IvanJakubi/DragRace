using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.UI;

public class ShopButtonSelected : MonoBehaviour
{

    [SerializeField] private Toggle tabButton;
    [SerializeField] private Toggle carButton;
    [SerializeField] private Toggle driverButton;
    // Start is called before the first frame update
    void Start()
    {
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
