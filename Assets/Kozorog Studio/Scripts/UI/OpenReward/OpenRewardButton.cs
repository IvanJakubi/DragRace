using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class OpenRewardButton : MonoBehaviour
{
    public int rewardStored;
    public CarsData carStored;
    public DriverData driverStored;
    public OpenRewardGroup rewardGroup;

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
